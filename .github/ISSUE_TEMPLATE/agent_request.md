---
name: Agent integration request
about: Support a new AI tool
labels: agent-integration
---

**Agent name and link**

**MCP support?** yes / no / partial

**Config file location**
- macOS:
- Linux:
- Windows:

**Config format** (paste an example with another MCP server already present)
```json
```

**Lifecycle hooks?** Does it expose SessionStart / PostToolUse equivalents? Link the docs if so — this determines whether automatic capture is possible.
